services:
  - type: web
    name: forge-food-ai
    runtime: python
    rootDir: food_ai_backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    plan: free
